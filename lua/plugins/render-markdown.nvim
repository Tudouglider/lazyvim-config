return {
  {
    "MeanderingProgrammer/render-markdown.nvim",
    opts = {
      -- 这里可以覆盖默认配置
      heading = {
        icons = { "◉", "○", "●", "○", "●", "○" }, -- 自定义标题图标
      },
      checkbox = {
        enabled = true, -- 启用复选框渲染
      },
    },
  },
}
